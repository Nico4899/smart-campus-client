import { ComponentTypePipe } from './component-type.pipe';

describe('ComponentTypePipe', () => {
  it('create an instance', () => {
    const pipe = new ComponentTypePipe();
    expect(pipe).toBeTruthy();
  });
});
