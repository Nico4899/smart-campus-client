import { ProblemStatePipe } from './problem-state.pipe';

describe('ProblemStatePipe', () => {
  it('create an instance', () => {
    const pipe = new ProblemStatePipe();
    expect(pipe).toBeTruthy();
  });
});
