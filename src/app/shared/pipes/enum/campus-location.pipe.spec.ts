import { CampusLocationPipe } from './campus-location.pipe';

describe('CampusLocationPipe', () => {
  it('create an instance', () => {
    // @ts-ignore
    const pipe = new CampusLocationPipe();
    expect(pipe).toBeTruthy();
  });
});
