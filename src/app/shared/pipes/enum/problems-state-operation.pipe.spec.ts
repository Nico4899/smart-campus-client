import { ProblemsStateOperationPipe } from './problems-state-operation.pipe';

describe('ProblemsStateOperationPipe', () => {
  it('create an instance', () => {
    const pipe = new ProblemsStateOperationPipe();
    expect(pipe).toBeTruthy();
  });
});
